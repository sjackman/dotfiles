#!/bin/sh

export NAME='Shaun Jackman'
export EMAIL='sjackman@gmail.com'

if command -v code >/dev/null; then
    export EDITOR='code'
    export VISUAL='code'
    export GIT_EDITOR='code -w'
else
    export EDITOR='vim'
    export VISUAL='vim'
    export GIT_EDITOR='vim -f'
fi

# Homebrew
export HOMEBREW_DEVELOPER=1
export HOMEBREW_GITHUB_USER=sjackman
export HOMEBREW_NO_AUTO_UPDATE=1
